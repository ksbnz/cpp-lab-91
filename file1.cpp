#include<iostream>
namespace sycs4
{
    int add()
    {
        int a,b,sum;
        std::cout<<"Enter two numbers";
        std::cin>>a>>b;
        std::cout<<"sum of the two numbers is"<<(a+b);
        return 0;
    }
    int subtraction()
    {
        int a,b,subtraction;
        std::cout<<"Enter two numbers";
        std::cin>>a>>b;
        std::cout<<"subtraction of the two numbers is"<<(a-b);
        return 0;
    }

}